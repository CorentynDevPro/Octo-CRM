{
  "name": "@octo-crm/billing-service",
  "version": "1.0.0",
  "private": true,
  "description": "Billing microservice — invoices, payments, subscriptions",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "fastify": "^5.7.2",
    "@octo-crm/types": "workspace:*",
    "@octo-crm/constants": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "vitest": "^1.4.0",
    "@octo-crm/config": "workspace:*"
  }
}
